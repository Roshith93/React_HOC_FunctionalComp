# Steps to start the project in loc

npm install
npm start

# Higher Order Component implementation steps

- Create a user list Component
- Add two states to it [users] and [terms]
- create api call for fetching data from JSON placeholder
- store it in state

- map the name
- add input field for search and add onchange handler to it
- filter the data with index of >=0
